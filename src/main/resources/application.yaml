server:
  port: 8080
spring:
  application:
    name: funnyutils  # 应用名称
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
#日志级别配置
logging:
  level:
    root: INFO
    com.zach.funnyutils: INFO